<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jiyun.wanandroids">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".base.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ATheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.home.registration.activity.RegistrationActivity"></activity>
        <activity android:name=".ui.home.login.activity.LoginActivity" />
        <activity android:name=".ui.wan.Wan_knowledge.Wan_Knowledge_Item.activity.Wan_Knowledge_Item_Activity" />
        <activity android:name=".util.WebActivity" />
        <activity android:name=".ui.home.activity.HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>